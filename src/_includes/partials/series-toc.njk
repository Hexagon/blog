<!-- Current Series TOC -->
{% if page.data.series %}
    <nav>
        <h4>{{ page.data.series.title }}</h4>
        <ul>
            {% for subpost in search.pages(page.data.series.tag, "part") %}
                <li><a class="{% if subpost.url == url %}link-secondary{% endif %}" href="{{ subpost.url }}">{{subpost.part}}. {{ subpost.title_short or subpost.title }}</a></li>
            {% endfor %}
        </ul>
    </nav>
{% endif %}